# Decision needed after `window6`

headline `window` = 1.5038030540556235, expected 0.82 +/- 0.6, verdict = WINDOW-CONFIRMED.

This is a direction-changing result. The queue halted so the next experiment's code can be rewritten before spending more compute. See window6_result.json.
